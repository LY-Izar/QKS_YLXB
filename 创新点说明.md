# 「医路相伴」核心创新点说明

## 一、项目概述与问题背景

「医路相伴」是一款面向老年慢性病患者及其家属的轻量化健康陪护 Web 应用。

**聚焦的社会痛点：** 截至 2025 年，我国 60 岁及以上老年人口超过 3 亿，其中 75% 患有至少一种慢性病。老年人在日常用药、指标监测、就医管理等方面普遍存在"记不住、不会用、看不清"的困境；与此同时，家属虽有关注意愿，但往往因不在身边、信息不对称而难以有效介入。

**项目定位：** 以"老人端简单易用 + 家属端远程看护"为核心理念，通过一套代码、一个页面，同时服务老人和家属两个角色，实现健康数据的本地管理与云端同步。

---

## 二、核心创新点

### 创新点一：单文件零构建架构——"打开即用"的健康应用新模式

**传统方案的局限：** 同类健康应用通常需要 Node.js/Vite/React 等构建工具链，部署流程复杂（npm install → npm run build → 服务器配置），对非专业运维人员不友好。

**本项目的创新：** 采用 **纯原生 HTML5 + CSS3 + ES2022 单文件架构**，整个应用（HTML 结构、CSS 样式、JavaScript 逻辑）均集中在单个 `index.html` 文件中（≈6500+ 行），实现：

- **零依赖安装**：无需 `npm install`、无需 Node.js 环境，所有第三方库（Chart.js、Vosk WASM 等）通过 CDN 引入
- **零构建编译**：无需打包、编译、压缩，源代码即部署产物
- **部署即开即用**：任意静态 HTTP 服务（Python `http.server`、Nginx、GitHub Pages、Supabase Storage）均可直接托管
- **可审计、可修改**：全量源代码透明可读，任何技术人员可快速定位并修改功能

**创新价值：** 极大降低了健康类 Web 应用的部署门槛和维护成本，使其能够在社区卫生中心、基层医疗机构等缺乏专业运维的场景中快速推广。

---

### 创新点二：双角色自适应 UI 架构——同一页面的"老人模式"与"家属模式"

**传统方案的局限：** 老人端应用通常采用极简大字设计，但家属看护端（数据密集、功能复杂）需要独立页面或独立 App，导致开发成本翻倍、用户切换不便。

**本项目的创新：** 通过 CSS 变量驱动 + 角色状态检测，在**同一套代码、同一个页面**上实现双角色自适应：

| 维度 | 老人端（`body.dark`） | 家属端（`body.care`） |
|---|---|---|
| 视觉模式 | 深色底 + 大号字体 + 大图标 | 明亮底 + 高亮数据卡片 |
| 功能入口 | 首页 7 大功能卡片（自查/用药/慢病等） | 家属绑定入口 + 老人健康汇总 |
| 导航栏 | 简洁按钮组，关怀模式下自动放大 | 紧凑导航，支持快速切换角色 |
| 数据写入 | 本地 IndexedDB + 云端脱敏同步 | **只读云端**，不写入本地老人数据 |
| 交互反馈 | 大字 Modal 提醒 + 语音朗读（Web Speech API） | 紧凑卡片 + 颜色编码预警 |

**关键技术实现：**
- 登录时根据 `role` 字段自动切换 `body.dark` / `body.care` CSS class
- CSS 变量（`--fs-h1`、`--radius`、`--primary` 等）统一驱动字号、圆角、配色
- `body.care .nav-link`、`body.dark .card` 等角色选择器精确控制样式覆盖
- 关怀模式（`body.care` 叠加）进一步放大字体 1.5-1.8 倍

**创新价值：** 在单文件架构下实现了"一套代码服务两类用户"的高效模式，既保证了老人端的极简易用性，又满足了家属端的数据访问需求，同时将开发维护成本降至最低。

---

### 创新点三：本地化优先 + 云端渐进同步的数据架构

**传统方案的局限：** 纯云端方案依赖网络稳定性，老年用户在弱网或离线场景下无法使用；纯本地方案无法实现多端数据同步（如老人在手机上记录，家属在电脑上看不到）。

**本项目的创新：** 设计了一套 **"本地 IndexedDB 优先 + Supabase 云端 RLS 渐进同步"** 的混合数据架构：

#### 3.1 本地化优先策略
- **IndexedDB 本地存储**：所有健康数据（用药记录、指标日志、慢病档案、事件提醒等）均优先写入浏览器本地 IndexedDB，即时响应、零延迟
- **离线可用**：Service Worker 缓存静态资源 + API 请求，在网络断开时仍可完整使用
- **账号隔离三件套**：`withUsername()`（写入时绑定账号）、`allMine()`（读取时过滤当前账号）、`ensureMine()`（删除前校验归属），确保多账号切换时数据绝不串档

#### 3.2 云端渐进同步
- **Supabase PostgREST**：通过 RESTful API（`sbFetch()` 封装）同步脱敏摘要数据到云端
- **RLS 行级安全策略**：每张数据表配置细粒度的 Row-Level Security 策略
  - `chronic_profiles`、`health_metrics` 等表：匿名用户可 CRUD（老人端自助写入）
  - `users` 表：仅管理员可全量访问（保护账号安全）
  - `family_bindings` 表：按 `family_id + elder_code` 唯一约束，防止重复绑定
- **家属端只读拉取**：家属通过 `family_code`（6-8 位邀请码）绑定老人后，仅通过云端只读查询老人的慢病摘要、指标趋势、预警列表，**绝不将老人数据写入家属本地 IndexedDB**
- **异步渲染令牌（`_nextRenderToken`）**：防止账号切换或快速操作时旧异步回调覆盖新数据

#### 3.3 多端实时同步
- **用药依从性**：老人端 `markMedTaken()` 写入本地 + 云端 `medication_logs`，家属端 `pullCloudEvents()` 每 15 秒轮询云端变更，实现依从性数据的近实时同步
- **云端公告系统**：管理员在 Supabase `announcements` 表发布公告，所有端（含未登录访客）在 1.5 秒内自动拉取并渲染顶部公告条与首访弹窗
- **邮箱广播**：管理员可通过 Supabase Edge Function（`broadcast_announcement`）调用 Resend API，向所有绑定邮箱的用户批量发送公告邮件

**创新价值：** 解决了"离线可用"与"多端同步"这两个看似矛盾的需求，既保障了老年用户在网络不稳定环境下的核心使用体验，又实现了家属远程看护所需的数据互通。

---

### 创新点四：用药依从性全流程闭环

**传统方案的局限：** 多数用药提醒应用仅提供定时通知，缺乏从"提醒 → 执行 → 记录 → 反馈"的闭环，导致实际依从率统计不准、家属无法了解老人真实服药情况。

**本项目的创新：** 构建了完整的用药依从性闭环管理：

#### 4.1 智能提醒系统
- **多层级提醒**：用药卡片在预定时间点前 10 分钟自动出现「已服用」「跳过今天」操作按钮
- **Web Audio API 警报**：到点未操作时，触发 880Hz 方波"滴滴"声（连续 10 秒），穿透老年人听力下降的障碍
- **大字 Modal 提醒**：同步弹出 48px 大字提醒框，显示药品名、剂量、服用时间，10 秒倒计时自动关闭
- **30 秒轮询机制**：`checkMeds()` 每 30 秒比对当前北京时间与每条用药的预定时间，匹配首次命中或每 10 分钟一档幂等触发

#### 4.2 依从性记录与统计
- **三档状态记录**：`taken`（按时服用）/ `late_taken`（迟到补服）/ `skipped`（跳过），精确记录 `actual_hhmm`（实际完成时间，北京时间 HH:MM）
- **30 天日历热力图**：以 5 种颜色编码（按时/补服/漏服/混合/无记录）直观展示每日依从情况
- **时间处理规范**：统一使用 `nowBeijing()` / `beijingYMD()` 函数，所有日期比较基于北京时间，彻底避免 UTC 与本地时区导致的日期错位问题
- **家属端同步**：家属端每 15 秒轮询云端 `medication_logs` 变更，实时看到老人的服药记录与依从率

#### 4.3 关怀友好交互
- **语音反馈**：老人每次操作完成后，`autoSpeak()` 通过浏览器 SpeechSynthesis API 播报"已记录 已服用"
- **强视觉反馈**：按钮点击后脉冲动画（`btnSuccessPulse`）+ 边框闪烁（`chipSuccessFlash`）+ 内联 Toast 提示
- **关怀模式增强**：`body.care` 模式下字体放大 1.5-1.8 倍，按钮最小尺寸从 40px 增至 54px

**创新价值：** 将"用药提醒"从单点通知升级为全流程闭环管理，通过技术手段显著提升老年慢性病患者的实际用药依从率。

---

### 创新点五：移动端自适应 + 关怀模式双层适配

**传统方案的局限：** 老年健康应用往往只做 PC 或单一移动端适配，缺乏对不同视觉能力老人的分层支持。

**本项目的创新：**

#### 5.1 全响应式布局
- **弹性网格系统**：主页宫格采用 `auto-fill minmax(240px, 1fr)` 自适应，宽屏自动扩展为 4-5 列，窄屏自动收缩为 1-2 列
- **三档断点适配**：
  - `≥1440px`（桌面宽屏）：main 容器 1600px，导航栏全宽，卡片横向展开
  - `768-1440px`（平板）：2 列网格，字号适中
  - `≤768px`（手机竖屏）：单列卡片，导航栏折叠为汉堡菜单
  - `≤480px`（小屏手机）：进一步收缩 padding 和字号
- **弹窗/对话框合理约束**：最大宽度 420-560px，不会随窗口无限拉宽

#### 5.2 关怀模式（一键切换）
- **全局字体放大**：标题 1.5 倍、正文 1.3 倍、按钮最小尺寸 54px
- **图标增大**：导航图标、卡片图标均放大至 48-58px
- **高对比度配色**：在深色模式下进一步提高文字与背景对比度
- **大触控区域**：所有按钮最小点击区域 54×54px，符合 WCAG 无障碍设计规范
- **角色联动**：关怀模式与家属端模式自动联动，一键开启后全局生效

**创新价值：** 一套代码同时覆盖桌面宽屏到小屏手机的全场景，并提供关怀模式作为"老人友好增强包"，有效解决了老年人视力衰退、手指不灵活等常见障碍。

---

## 三、与现有方案的差异化对比

| 对比维度 | 普通健康 App（移动端） | 普通 Web 健康应用 | 「医路相伴」 |
|---|---|---|---|
| 部署复杂度 | 需 App Store/审核 | 需 Node/Vite 构建 | **零构建，静态托管即开即用** |
| 双角色支持 | 独立 App 或独立账号 | 独立页面 | **同页面 CSS 自适应切换** |
| 离线能力 | 有限 | 依赖网络 | **IndexedDB 全离线可用** |
| 多端同步 | 云同步（依赖网络） | 云同步（依赖网络） | **本地优先 + 渐进云端** |
| 用药依从性 | 定时通知为主 | 定时通知为主 | **提醒→执行→记录→反馈闭环** |
| 老年友好 | 大字（部分） | 常规设计 | **关怀模式 + 语音反馈** |
| 家属看护 | 需独立登录 | 需独立登录 | **邀请码绑定 + 只读看护** |
| 管理员后台 | 一般无 | 一般无 | **覆盖率徽章 + 云端公告** |

---

## 四、技术实现亮点

### 4.1 单文件工程组织
```
index.html (≈6500+ 行)
├── <style>   (全量 CSS，按功能分区注释)
├── <body>    (语义化 HTML 结构)
└── <script>  (全量 JavaScript，函数名即文档)
    ├── 状态管理（currentUser, myRole, cloudUsable）
    ├── 数据层（IndexedDB 封装 + Supabase REST 封装）
    ├── 渲染层（renderMeds, renderCompliance, drawFamilyChart）
    ├── 交互层（事件委托、强反馈动画、语音合成）
    └── 路由层（go(view) + 视图切换）
```

### 4.2 防串数据三件套
```javascript
// 写入时：自动绑定当前账号
const record = withUsername({ med_id, name, dose, time });
DB.put('meds', record);

// 读取时：过滤仅当前账号
const myMeds = await allMine('meds'); // 内部自动 filter by currentUser

// 删除时：校验数据归属
await ensureMine('meds', id); // 非本人数据直接报错
```

### 4.3 时间处理规范
```javascript
// 唯一合法的时间基准（避免 UTC/本地时区混乱）
nowBeijing()        → Date（当前北京时间）
beijingYMD(date)    → "YYYY-MM-DD"（北京时间年月日字符串）
beijingHHMM(date)   → "HH:MM"（北京时间时分字符串）

// 依从性日历 30 天窗口（已按此规范重写）
for (let i = 0; i < 30; i++) {
  const d = nowBeijing() - i * 86400000; // 基于北京时间减天数
  const ds = beijingYMD(d);               // 日期统一基准
  const logs = myLogs.filter(l => l.scheduled_date.slice(0, 10) === ds);
}
```

### 4.4 异步渲染令牌
```javascript
let _nextRenderToken = 0;
function bumpRenderToken(reason) { return ++_nextRenderToken; }

async function renderMeds() {
  const myToken = _nextRenderToken; bumpRenderToken('renderMeds');
  // ... 异步操作 ...
  if (myToken !== _nextRenderToken - 1) return; // 已过时，丢弃
}
```

### 4.5 管理员后台能力
- **邮箱绑定覆盖率徽章**：`admin_count_emails` RPC 统计 + 兜底直接查询 users 表，颜色编码（绿≥60% / 黄 30-59% / 红<30%）
- **云端公告多端同步**：`announcements` 表（PostgREST）+ RLS（管理员写、公开读）+ Supabase Edge Function 批量邮件推送
- **一键清理测试数据**：`purgeTestAccounts()` 级联删除 `test_` 前缀账号及其所有关联数据

---

## 五、社会效益展望

1. **降低老年数字鸿沟**：通过关怀模式、语音反馈、极简操作，让 60+ 老年用户能够独立使用
2. **缓解家属照护压力**：远程查看老人健康数据，减少电话问询次数，让在外工作的子女安心
3. **基层医疗赋能**：单文件架构可快速部署在社区卫生中心、养老机构等场景
4. **慢病管理闭环**：用药依从性闭环 + 指标趋势记录 + 复诊提醒，形成慢病自我管理的完整工具链
5. **隐私保护**：账号隔离三件套 + RLS 行级安全 + 本地优先存储，最大限度保护老人健康数据隐私

---

**文档结束**  
*"医路相伴"—— 一套代码，两代守护，让科技真正陪在身边。*
