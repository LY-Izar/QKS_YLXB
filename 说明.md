# 医路相伴 — 项目说明（供后续 Agent 接手）

> 本文档目标：让新的 AI Agent 无需翻历史对话，**直接从文件代码 + 本文档** 读懂项目结构、技术栈、功能模块、数据模型与容易踩坑的约定，快速定位功能实现位置并正确修改。

---

## 1. 项目概览

「**医路相伴**」是一个**面向老年人 + 家属双角色**的轻量级**健康陪护 Web 单文件应用**（纯 HTML + CSS + JS，**没有 Node/Vite/React 打包层**）。它在浏览器本地 IndexedDB 里存个人数据，并通过 Supabase PostgREST 云接口把脱敏摘要同步到云端，家属端输入老人邀请码（family_code）后即可**只读查看**老人的慢病/指标/用药/事件。

- 部署方式：**静态 HTTP 服务**即可（Python `http.server` / Nginx / Supabase Storage / GitHub Pages），可配合 `manifest.json` + `sw.js` 成为 PWA 离线可用。
- 后端存储：**Supabase（Postgres + PostgREST + RLS）**，无自建 API；所有云操作走 `https://hjryfgujkxuaxovftlai.supabase.co/rest/v1`（anon key）。
- 两种角色：**老人端（`body.dark` 深色 + 大字/大图标 日间简洁）**；**家属端（`body.care` 明亮色 + 高亮老人数据）**，通过 `myRole` 状态和 `go('family')` 绑定切换。
- 核心功能 7 大视图：**首页 / 症状自查 / 就医流程 / 用药提醒 / 慢病管理 / 找医院 / 账户**，辅助视图：**药品库 / 家属绑定页 / 登录注册页**。

---

## 2. 目录与关键文件（全部本地）

```
a/
├── index.html                     # ⭐ 唯一主文件（HTML + CSS + <script> 全部 JS 都在这里，≈6500+ 行）
├── manifest.json                  # PWA manifest（离线安装、图标、启动 URL）
├── sw.js                          # Service Worker：静态资源 + API 请求离线缓存
├── vosk.js                        # 本地离线语音识别封装（Vosk WASM）—— 自查语音输入
├── 启动服务器.command              # macOS 下双击启动本地 8000 端口 HTTP(S) 服务脚本
│
├── 动画/                           # 卡片光效配置代码（CSS.txt/代码.txt/配置.txt，共 7 套样式，供 index.html 拷贝使用）
│   └── 1 ~ 7/
│
├── supabase/
│   ├── migrations/                # ⚠️ 所有 Supabase 建表/加字段脚本，直接粘贴到 SQL Editor 执行
│   │   ├── add_chronic_disease.sql     # 慢病 5 表 + RLS anon all 策略（核心建表）
│   │   ├── add_family_binding.sql      # 绑定关系：users.bound_elder_code / events.user_id / family_bindings 表
│   │   ├── add_multi_binding.sql       # 一家属绑定多老人：users.bound_elder_codes TEXT[]
│   │   └── add_push_notification_fields.sql
│   └── functions/
│       └── medication-reminder/
│           └── index.ts             # Edge Function：用药提醒推送（Deno，可选部署，当前应用定时提醒主要在浏览器 30s 轮询完成）
│
├── LICENSE, README.md, .gitignore
```

> ⚠️ **修改核心代码只需要编辑 `index.html`**；其他文件极少改动。

---

## 3. 技术栈（无构建层）

| 层 | 选型 | 关键代码位置 |
|---|---|---|
| 前端 | 原生 HTML5 / CSS3 / ES2022（无 React/Vue/jQuery，DOM 手写） | [index.html](file:///c:/Users/Administrator/Desktop/a/index.html) 全部 |
| 图表 | **Chart.js v4**（通过 CDN `<script src="https://cdn.jsdelivr.net/npm/chart.js@4">`），慢病趋势图与家属端趋势图 | [drawFamilyChart](file:///c:/Users/Administrator/Desktop/a/index.html#L5232-L5338) ，[renderChart / _renderChartCore](file:///c:/Users/Administrator/Desktop/a/index.html#L6054-L6176) |
| 本地数据库 | **IndexedDB v2**（自定义 `DB = {get,put,del,all,keys}` 封装 Promise） | [const DB](file:///c:/Users/Administrator/Desktop/a/index.html#L2601-L2960) |
| 云端数据库 | **Supabase PostgREST**（POST/GET/PATCH/DELETE 直接 `fetch`，不用 supabase-js） | [SB_URL / SB_KEY](file:///c:/Users/Administrator/Desktop/a/index.html#L3462-L3463) ，通用 [`sbFetch(path, opts)`](file:///c:/Users/Administrator/Desktop/a/index.html#L3471-L3540) |
| 账号/加密 | 密码走轻量 SHA256，存储 `users.pass_hash`；**无 JWT 登录态**，登录后 username = currentUser 存在内存 + 可选 localStorage | [quickUse](file:///c:/Users/Administrator/Desktop/a/index.html#L3756) / [users 读写](file:///c:/Users/Administrator/Desktop/a/index.html#L3625-L3738) |
| 地图 | **高德地图 AMap Web API**（key 内嵌 JS）定位 + 医院周边搜索 | [renderHospitalsAMap](file:///c:/Users/Administrator/Desktop/a/index.html#L4932-L5043) |
| 语音朗读 | **浏览器 SpeechSynthesis + autoSpeak()** 老人端自动播报操作结果、提醒标题 | [autoSpeak](file:///c:/Users/Administrator/Desktop/a/index.html#L2970-L3030) |
| 用药提醒警报 | **Web Audio API 方波 880Hz "滴滴"声**（10 秒连续） | [playDidiAlert](file:///c:/Users/Administrator/Desktop/a/index.html#L4545-L4577) |
| 样式主题 | CSS 变量驱动：`body.dark` 暗夜模式；`body.care` 家属端高亮模式；移动端响应式 | [index.html `<style>`](file:///c:/Users/Administrator/Desktop/a/index.html#L1-L940) |
| 离线 | PWA（Service Worker + manifest） | [sw.js](file:///c:/Users/Administrator/Desktop/a/sw.js) / [manifest.json](file:///c:/Users/Administrator/Desktop/a/manifest.json) |

---

## 4. 页面路由与 7 大功能模块（`go(view)` 切换视图）

顶层路由函数：[`async function go(view)`](file:///c:/Users/Administrator/Desktop/a/index.html#L3431-L3540)。它：
- 给对应 `.view[id=view-XXX]` 加 class `active`（其他隐藏）
- navbar `.nav-link[data-v=xxx]` 高亮
- 切到特定视图时会主动**重渲染需要刷新的区块**（例如 `go('med')` → `await renderMeds(); renderCompliance();`）

### 视图清单（HTML id → 路由名 → 对应功能）

| 视图 HTML id | 路由名 go('X') | 功能说明 | 关键函数/位置 |
|---|---|---|---|
| `#view-welcome` | welcome / 默认首屏 | 登录 / 注册 / 家属端入口 / 快速体验（无需账号） | [quickUse](file:///c:/Users/Administrator/Desktop/a/index.html#L3756-L3880) |
| `#view-home` | home | **首页**：7 大功能卡片入口，天气/日期问候；老人端 vs 家属端文案不同 | [index.html#L994](file:///c:/Users/Administrator/Desktop/a/index.html#L994-L1007) |
| `#view-symptom` | symptom | **症状自查**：①文字输入（或 Vosk 语音）→ ②人体部位选择区 + 症状选中态 `on` + `✓` 前缀 → ③结果：科室/轻重/处置建议 | [bodyPartClick](file:///c:/Users/Administrator/Desktop/a/index.html#L1527-L1595) / [renderSyms](file:///c:/Users/Administrator/Desktop/a/index.html#L3887-L4030) |
| `#view-flow` | flow | **就医流程**：按步骤图文引导（挂号→候诊→就诊→检查→缴费→取药→住院流程） | [index.html#L1042](file:///c:/Users/Administrator/Desktop/a/index.html#L1042-L1056) |
| `#view-druglib` | druglib | **药品库（辅助）**：浏览常见药品名、适应症、注意事项，从用药提醒页跳转进入 | [index.html#L1106](file:///c:/Users/Administrator/Desktop/a/index.html#L1106-L1124) |
| `#view-med` | med | **用药提醒（⭐核心）**：新增药品+频次+时间 → 每日按时间生成"已服用/已补用/跳过今天"按钮 → 30 秒轮询超时提醒 → 依从率+30天日历热力图 | 见 4.1 |
| `#view-chronic` | chronic | **慢病管理（⭐核心）**：创建/切换/删除慢病档案（强制先选档案后录入）→ 指标记录录入+紧急高亮 → 近30天趋势图 → 复诊提醒；家属端只读查看对应老人 | 见 4.2 |
| `#view-hospital` | hospital | **找医院**：本地三甲医院白名单 + 高德 API 定位搜索；**资质过滤**（排除诊所/专科医院/美容机构/保健中心） | [isQualifiedGeneralHospital](file:///c:/Users/Administrator/Desktop/a/index.html#L4700-L4720) / [validateHospitalInput](file:///c:/Users/Administrator/Desktop/a/index.html#L4722-L4760) / [renderHospitals](file:///c:/Users/Administrator/Desktop/a/index.html#L4889-L4930) / [renderHospitalsAMap](file:///c:/Users/Administrator/Desktop/a/index.html#L4932-L5043) |
| `#view-family` | family | **家属绑定 / 切换**：老人端生成邀请码；家属端输入邀请码绑定 → 显示慢病汇总卡片+预警+趋势图 | [bindFamily](file:///c:/Users/Administrator/Desktop/a/index.html#L5504-L5570) / [renderChronicSummary](file:///c:/Users/Administrator/Desktop/a/index.html#L5340-L5500) / [pullCloudEvents](file:///c:/Users/Administrator/Desktop/a/index.html#L5055-L5160) |

---

### 4.1 用药提醒模块（视图 `view-med`）

```
┌─ 表单：药名 / 剂量 / 频次（每日/每周N天/每隔X天）/ 多个时间点 / 备注  ➕ 保存
├─ 每条药品卡片（.med-item，flex 纵向）
│   ├─ 第一行：药品名+剂量+备注 + 删除按钮
│   └─ 第二行（chips 独占一行，避免挤压）：每个时间点 chip
│        ├─ 03:49 之前：时间 chip 正常
│        ├─ 到点 ～10 分钟：「已服用」「跳过今天」 两枚按钮
│        └─ 超过 10 分钟未操作：「已补用」「跳过今天」（颜色变黄；迟到分钟数 badge）
└─ 我的用药依从性（卡片）：
     依从率% / 按时N / 补服N / 漏服N 三档徽章
     近 30 天 5 色日历热力图（按时/补服/漏服/混合/无记录）
```

#### 关键函数与约定

| 函数 | 位置 | 作用 |
|---|---|---|
| `renderMeds()` | [L4186](file:///c:/Users/Administrator/Desktop/a/index.html#L4186-L4320) | 主渲染。按 meds 生成卡片+时间 chip+按钮；**按钮不写内联 onclick**，用 data 属性 `data-med-id / data-med-date / data-med-time / data-med-act` + 全局委托捕获 |
| `registerMedActionDelegation()` | [L4323](file:///c:/Users/Administrator/Desktop/a/index.html#L4323-L4430) | `document.body` 一次性 `addEventListener('click')` 委托：根据 data 属性分发到 `markMedTaken` 并加按钮强反馈动画 + inline toast |
| `markMedTaken(medId,date,timeSlot,status)` | [L6260](file:///c:/Users/Administrator/Desktop/a/index.html#L6260-L6350) | 核心写库：写入 IndexedDB `medlog` + Supabase `medication_logs`（脱敏）；status ∈ { taken, late_taken, skipped }；同时补存 `actual_hhmm`（北京时间，用于 badge 显示）；末尾 `await renderMeds()` + `renderCompliance()` 后再插内联 toast，避免 render 重建 DOM 时把刚插入的 toast 清空 |
| `renderCompliance(targetUser,statsId,calId)` | [L6077](file:///c:/Users/Administrator/Desktop/a/index.html#L6077-L6258) | 依从率+日历渲染：30 天窗口按北京时间过滤，日期计算统一 `nowBeijing()` / `beijingYMD(d)`，保证下方标签 M/D 和格子的 scheduled_date 对齐（不然凌晨 UTC 差 1 天会错位） |
| `checkMeds()` | [L4614](file:///c:/Users/Administrator/Desktop/a/index.html#L4614-L4700) | **每 30 秒轮询**一次：对比当前北京时间 HH:MM 与每条药时间，匹配「首次命中或每 10 分钟一档」幂等计数 → 触发提醒 |
| `showMedAlertUI(titleMsg, hintMsg, onClose)` | [L4579](file:///c:/Users/Administrator/Desktop/a/index.html#L4579-L4612) | 大字 Modal 提醒（纯文本参数，**不要传 HTML 字符串**，避免 `<b>` 被转义后原文显示）；10 秒倒计时倒计时自动关闭 |
| `playDidiAlert(10)` | [L4545](file:///c:/Users/Administrator/Desktop/a/index.html#L4545-L4577) | 每次提醒配套 10 秒滴滴声（880Hz 方波 Web Audio） |

---

### 4.2 慢病管理模块（视图 `view-chronic`）

> 设计原则：**"老人端 — 不选档案不让录入"+"当前档案强提示+空态引导"**；家属端 — 高亮即可，不把老人数据写入本地 IndexedDB。

#### 档案区（顶部）
- 老人端：卡片列表，选中档案高亮（日间/深夜各自强视觉色），未选中档案时**指标录入区/趋势图区显示空态提示**。
- 家属端：只读展示老人绑定的慢病档案 + 确诊医院；不写入本地 `chronic` store。

#### 指标录入 & 列表
- 录入前必须 `selectedChronicId` 有值，否则不开放表单并提示"请先选择档案"。
- 每次录入后列表按紧急性 `alert_level` 上色（正常绿/超黄/危红），近 90 天逐条展示。
- 紧急值同时写一条 `chronic_alerts`（含 is_read = false），家属端首页汇总卡片最多显示未读 10 条。

#### 趋势图（Chart.js v4，Canvas）
- 老人端 `chronicChart`（单选指标下拉），家属端 `familyChart`（按最近各指标值叠加）。
- 图表容易空白的历史坑（已全部修复，但修改要小心）：
  - 👉 **容器 `display:none` 时 canvas 宽=0 不能初始化** — 所以用 DOM 层空状态 `<div id="chronicChartEmpty">` 先显示文字，再 `setTimeout` 多级重试 + 固定保底 width=800px 创建。
  - 👉 **`document.body` 未挂载时不能 `body.classList.contains('care')`**，统一改为 `document.body && document.body.classList.contains('care')`。
  - 👉 **切视图/重渲染必须先 `chart?.destroy()` 旧实例**，否则重复创建导致内存泄漏/图层重叠/空白。

#### 复诊提醒
- 每条档案 0～N 条，`reminder_days`（默认 7）内未完成的，`init()` / `go('chronic')` 时弹出提醒。
- `completeFollowup(id)` 写本地 + 云端 `follow_ups` 表。

#### 家属端汇总（绑定老人后首页/慢病页）
- [renderChronicSummary(elderCode)](file:///c:/Users/Administrator/Desktop/a/index.html#L5340-L5500)
  - 拉 3 个 Supabase 请求（health_metrics ≥ 30 天内 / chronic_alerts 未读 ≤10 / chronic_profiles 基础信息）
  - 三个请求结果汇总为 3 张卡片：**慢病档案卡片 / 最近指标卡片 / 未读预警卡片**
  - 下方 `familyChart`（独立 Canvas）
  - 下方「全部指标记录」90 天列表（每条含紧急色）

---

## 5. 账号体系、角色与「**绝不串数据**」约定（重要！）

### 5.1 全局状态（内存）
登录/快速体验/家属绑定后，会在内存写入以下变量（无 sessionStorage 持久化；localStorage 仅存储当前用户名，用 `lsKey()` 加后缀）：

| 变量 | 类型 | 说明 |
|---|---|---|
| `currentUser` | string | 当前登录用户名（= users.id），未登录空串或 'guest' |
| `myRole` | 'elder' \| 'family' | 老人端=elder；家属端=family；**`body.care` class 与 role=family 同步切换**，家属端的文案/导航用 `.care` CSS 区分 |
| `family_code` | string | 我/我绑定老人的邀请码（6-8 位短码） |
| `bound_elder_code / bound_elder_codes[]` | string \| array | 家属端"绑定的一位老人"向后兼容字段 + 新多老人数组 |
| `cloudUsable()` | boolean | `SB_KEY` + 网络可访问 = true；false 时整个云同步逻辑走分支 |

### 5.2 防串数据 — 账号隔离三件套（必须用，禁止直接 `DB.all('meds')` 全表扫）

这是项目最容易踩坑的地方之一（之前多个版本出过"同机切换账号看到别人数据"的 bug，根因就是直接读整个 store 没按 username 过滤）。

定义在 [L2407-L2460](file:///c:/Users/Administrator/Desktop/a/index.html#L2407-L2460)，所有读写 IndexedDB 的地方**必须使用**：

| 函数 | 用法 | 作用 |
|---|---|---|
| `withUsername(rec)` → `{...rec, username: currentUser}` | 写入前套一层 | 确保所有新增记录都带 `username = currentUser`；老人端 meds/medlog/chronic/metrics/followups/events 均带此字段 |
| `allMine(storeName)` → `Promise<Record[]>` | 读：取代 `DB.all(storeName)` | 返回 `DB.all(storeName).filter(r => r.username === currentUser)`，只读当前账号的行 |
| `ensureMine(storeName, id)` → `Promise<Record>` | 删除前校验归属 | 先 `DB.get(storeName, id)` 取记录，校验 `record.username === currentUser`，不一致抛错不删除 |
| `lsKey(base)` → `${base}_${encodeURIComponent(currentUser)}` | localStorage 键生成 | 同一台机器多账号切换下，每个 localStorage key 也用账号后缀隔离 |

**家属端不能写老人数据到本地！**
- 家属端 `renderChronicSummary` 只读云端，不把返回结果 `DB.put` 到本地 `chronic/metrics/medlog/events` store。
- 家属端若曾错误写入，只要全部通过 `allMine/ensureMine` 读/删，家属自己的 username 过滤后是看不到老人数据的（双保险）。

### 5.3 异步并发保护 — `_nextRenderToken` 渲染令牌

异步渲染（renderMeds / renderCompliance / pullCloudEvents 等）需要时间；但如果中途**切账号**或**用户再次触发渲染**，旧的异步回调仍会往 DOM 写脏数据，把新账号的 DOM 覆盖。

解决：L2417 [`bumpRenderToken(reason)`](file:///c:/Users/Administrator/Desktop/a/index.html#L2417) + 每个 render 函数开头 `const myToken = _nextRenderToken; bumpRenderToken('xxx');`，异步回调完成后判断 `if(myToken !== _nextRenderToken-1) return` 把过期结果丢弃（会打 debug log）。

所有**账号切换/退出**时（如 `resetAppState`），`bumpRenderToken` 会清零所有挂起渲染。

---

## 6. 数据存储模型

### 6.1 本地 IndexedDB（库名 `QKS_DB`，版本 1）

在 [DB 初始化代码](file:///c:/Users/Administrator/Desktop/a/index.html#L2601-L2700) 建表：

| Store 名 | 存什么 | 主键 | 归属字段 |
|---|---|---|---|
| `users` | 本机缓存账号（非全部云端账号） | `id`（= 用户名） | — |
| `meds` | 用药提醒条目（药品+时间+剂量） | `id`（自增或 `${name}_${time}`） | `username`（必须 withUsername 写入） |
| `medlog` | 每次服药/漏服结果（用于依从性） | `id` | `username` + `scheduled_date, scheduled_time, status` |
| `chronic` | 慢病档案（疾病+确诊日+医院+目标值JSON） | `id` | `username` |
| `metrics` | 血压/血糖/血脂等指标录入记录 | `id`（自增） | `username + disease_type + metric_type + measured_at` |
| `followups` | 复诊提醒 | `id` | `username + disease_type + next_visit_date` |
| `events` | 通知/事件（紧急提醒） | `id` | `username 或 family_code` |
| `sym_cache` | 症状选择缓存 | `id` | —（本地即时使用，不用账号隔离） |
| `qkshist` | 自查历史记录 | `id` | `username` 可选 |

### 6.2 Supabase 云端表（Postgres）

所有表建在 `public` schema，迁移脚本在 `supabase/migrations/*.sql`，项目使用的表：

| 表 | 关键字段 | RLS 策略 | 对应本地 store |
|---|---|---|---|
| **users** | id(PK) / pass_hash / role / family_code / bound_elder_code / bound_elder_codes TEXT[] / created_at | 有（需 service_role 或 dashboard 才能全删） | users（仅本机缓存） |
| **chronic_profiles** | id / username / disease_type / diagnose_date / hospital / target_json / created_at | ✅ anon all（匿名 CRUD 全部行） | chronic |
| **health_metrics** | id / username / disease_type / metric_type / value1,value2 / measured_at / note / created_at | ✅ anon all | metrics |
| **medication_logs** | id / username / med_name / scheduled_date(TEXT=YYYY-MM-DD 北京) / scheduled_time / status ∈ {taken, late_taken, skipped} / actual_hhmm / actual_time / created_at | ✅ anon all | medlog |
| **chronic_alerts** | id / username / disease_type / alert_level / title / message / is_read / created_at | ✅ anon all | 派生（本地不持久化） |
| **events** | id / family_code / type / text / created_at | 有（按 family_code 可读；否则匿名能读自己创建的） | events |
| **follow_ups** | id / username / disease_type / next_visit_date / department / hospital / notes / reminder_days / is_completed / created_at | ✅ anon all | followups |
| **family_bindings** | id(PK) / family_id / elder_code / created_at，UNIQUE(family_id, elder_code) | 有（增量绑定，客户端能写自己的） | — |

> 注：迁移脚本里 `CREATE POLICY "anon all chronic_profiles" ON chronic_profiles FOR ALL USING (true) WITH CHECK (true);` 这 5 张慢病表允许匿名任意行读写，**所以客户端能清云端调试数据**。users/events 因为 RLS 更严格只能通过 Supabase SQL Editor TRUNCATE。

---

## 7. 时间处理规范（❗踩坑最多，必须严格遵守）

**项目之前一半的 bug 来自"混用 UTC 和北京时间 + 浏览器解析 YYYY-MM-DD 歧义"。请严格按本节！**

### 7.1 唯一合法的时间函数（在 [L4082-L4096](file:///c:/Users/Administrator/Desktop/a/index.html#L4082-L4096) 定义）

| 函数 | 用途 | 返回格式 |
|---|---|---|
| `nowBeijing()` → `Date` | "当前北京时间" Date 对象（注意：浏览器 new Date 也是北京时间时等同，**显式调用让代码表达意图**） | Date 实例（timestamp +8 小时以对齐中国时区） |
| `beijingYMD(date)` → 字符串 | 取任意 Date 的"北京时间年月日" | `YYYY-MM-DD` |
| `beijingHHMM(date)` → 字符串 | 取任意 Date 的"北京时间时分" | `HH:MM` |

> 禁止：
> - ❌ `Date.toISOString().slice(0,10)` — 会截成 UTC 日期！凌晨（北京 00:00-07:59）时，UTC 还是前一天，日期少 1 天（用药依从性日历错位根源）。
> - ❌ `new Date("2026-08-26")` — ES 规范下这串被解析为 **UTC 零点**，等于北京 08:00；在跨天比较时非常容易出错。**一律先用字符串 beijingYMD 比大小**。
> - ❌ 用 `getDate()/getMonth()/getYear()` 去和 UTC 日期比较（基准不一致）。

### 7.2 存什么？

| 字段名 | 内容 | 生成方式 | 用在哪 |
|---|---|---|---|
| `medlog.scheduled_date` | "今天是哪一天（北京）" | `beijingYMD(nowBeijing())` | 依从性日历 30 天过滤 + 格子着色匹配；家属端云端拉取 |
| `medlog.scheduled_time` | "哪个服药时段（HH:MM 北京）" | 用户表单选的 `<input type=time>` 直接是 HH:MM | — |
| `medlog.actual_hhmm` | "实际完成/跳过的时间（HH:MM 北京）" | `beijingHHMM(nowBeijing())` | 按钮旁 badge 显示（**不要**从 actual_time 的 UTC 截 11:16！） |
| `medlog.actual_time` | 标准 ISO UTC 时间戳 | `new Date().toISOString()` | 云端审计排序用，**从不直接截取给用户看** |
| `followups.next_visit_date` | 复诊日 | `YYYY-MM-DD` 字符串（表单 `<input type=date>` 北京时区选的就是本地日期，直接存） | 提前 N 天提醒 |
| `metrics.measured_at` | 测量时间 | TIMESTAMPTZ（Postgres）或 ISO 字符串 | 趋势图 X 轴排序（趋势图 X 轴用真实时刻坐标，一般无跨天问题） |

### 7.3 依从性日历 30 天窗口计算（已按本规范重写）

[renderCompliance 日历段](file:///c:/Users/Administrator/Desktop/a/index.html#L6177-L6214)

```
now = nowBeijing()
for 29..0:
  d = now - i*86400000        （注意：基于北京时间 Date 减天数，避免 UTC 跨天）
  ds = beijingYMD(d)           （日期筛选基准 = scheduled_date 存储格式）
  bm = d.getMonth()+1, bd = d.getDate()
  label = `${bm}/${bd}`        （下方 M/D 标签和 ds 来自同一 Date 源 → 绝不错位）
  dayLogs = myLogs.filter(l => l.scheduled_date.slice(0,10) === ds)
```

**修改/新增任何依从性/日期相关的代码，都优先用 beijingYMD / nowBeijing 计算。**

---

## 8. 医院资质过滤规范

用户要求：**只保留正规综合资质医院**，排除专科医院/医疗美容/保健中心/小型诊所/体检中心/中医保健等。

定义在 [L4700-L4760](file:///c:/Users/Administrator/Desktop/a/index.html#L4700-L4760)：

| 函数 | 作用 |
|---|---|
| `isQualifiedGeneralHospital(h)` | 对本地医院列表/高德返回的每条医院，**必须经过**才能进入展示列表。逻辑：白名单关键词命中（人民医院/中心医院/附属/协和/军医/总医院...）优先通过；命中黑名单（诊所/口腔/眼科/美容/体检/月子/宠物/保健...）直接排除；名称长度 <3 的异常值也排除；最终用正则返回 true/false |
| `validateHospitalInput(name)` | 慢病档案 / 复诊提醒录入时**用户手动输入的医院名**，保存前必须通过此函数：否则 toast 提示 `医院名称不符合综合医院要求，请选择正规综合医院` 并阻止保存；同时把历史记录里的异常输入（专科医院/诊所）在展示时统一标记为⚠️ 并给出修改入口 |

> 不希望出现 "美容门诊 XX 诊所" 被老人误选。如果要放宽，**只在白名单正则里加关键词**，不要动黑名单。

---

## 9. 按钮/事件委托 & 强反馈约定

**不要在 HTML `onclick` 字符串里传动态参数**（如 `onclick="markMedTaken('${id}','${date}')"`），之前因为引号嵌套 / 中文字符串转义出过多次按钮无反应 bug。

统一做法：
1. 写入元素 `data-med-id`, `data-med-date`, `data-med-time`, `data-med-act` 四个 data 属性（纯字符串）。
2. [`registerMedActionDelegation()`](file:///c:/Users/Administrator/Desktop/a/index.html#L4323-L4430) 注册一次性 `body` 点击委托，读取 e.target 的 `closest('[data-med-id]')` 取四个属性，分发给 `markMedTaken`。

强反馈（用户点击后必须立刻感知）：
- 点击按钮：临时加 `btnSuccessPulse` 类（CSS @keyframes 脉冲动画）
- 对应 chip 容器：根据结果加 `chipSuccessFlash` / `chipSkipFlash`（边框闪烁）
- 卡片内插入 `med-inline-toast`（小浮层，带操作结果文字，4.2 秒自动消失，避免顶部 toast 被页面滚走）
- 老人端 `myRole !== 'family'` 时额外调用 `autoSpeak('已记录 已服用')` 语音播报

---

## 10. 启动与调试方式

### 10.1 启动本地服务器
项目不能直接 `file://` 打开（因为 Service Worker / IndexedDB / fetch Supabase 会被浏览器安全策略拦截），必须启 HTTP 服务：

```bash
# Windows PowerShell / cmd / macOS 通用
cd a
python -m http.server 8000
# 或双击 启动服务器.command（macOS）
```
然后浏览器访问 `http://127.0.0.1:8000/index.html`。

### 10.2 快速调试入口
- 首屏点击「快速体验」= [`quickUse()`](file:///c:/Users/Administrator/Desktop/a/index.html#L3756)，会生成一个 guest 用户名，直接进入老人端，无密码。
- 家属端：登录/注册时选角色"家属"→ go('family') → 输入邀请码绑定老人。
- 浏览器控制台可直接调用：`go('med') / renderCompliance() / checkMeds() / DB.all('meds').then(console.log)`，所有顶层函数都是 window 全局函数。

### 10.3 不要直接在浏览器 DevTools 删 IndexedDB（除非调试）
正式清理个人数据：账户页点击"退出登录" → 触发 [`resetAppState()`](file:///c:/Users/Administrator/Desktop/a/index.html#L3547)，会 destroy 所有图表实例、clear 敏感输入框、`bumpRenderToken` 作废异步回调、然后 clear localStorage / sessionStorage。

### 10.4 清空云端调试数据
慢病相关表可通过 Supabase REST 清（anon all 允许），但 users/events 受 RLS 保护，登录项目 Supabase Dashboard → SQL Editor 运行：

```sql
TRUNCATE TABLE users              RESTART IDENTITY CASCADE;
TRUNCATE TABLE events             RESTART IDENTITY CASCADE;
TRUNCATE TABLE family_bindings    RESTART IDENTITY CASCADE;
TRUNCATE TABLE chronic_profiles   RESTART IDENTITY CASCADE;
TRUNCATE TABLE health_metrics     RESTART IDENTITY CASCADE;
TRUNCATE TABLE medication_logs    RESTART IDENTITY CASCADE;
TRUNCATE TABLE chronic_alerts     RESTART IDENTITY CASCADE;
TRUNCATE TABLE follow_ups         RESTART IDENTITY CASCADE;
```

---

## 11. 关键修改快速索引（按常见需求找函数）

| 你想要改 | 看这里 |
|---|---|
| 首页布局 / 7 个 tile / 家属端首页 | [L994 `#view-home`](file:///c:/Users/Administrator/Desktop/a/index.html#L994-L1007) ，老人/家属分支写在初始化 |
| 症状自查流程 / 部位选择高亮 / 选中态双向同步 | [bodyPartClick](file:///c:/Users/Administrator/Desktop/a/index.html#L1527) + [renderSyms](file:///c:/Users/Administrator/Desktop/a/index.html#L3887) |
| 用药卡片布局（时间 chips 和按钮的换行/挤压问题） | [renderMeds HTML](file:///c:/Users/Administrator/Desktop/a/index.html#L4186)，核心是 `.med-item` 强制 `flex-direction:column` + topRow/chipsWrap 两行 |
| 新增一条用药提醒保存逻辑 | `addMed()` → 在 renderMeds 之后搜 `addMed` 对应函数 |
| 依从性日历（30 天配色/格子/标签对齐） | [renderCompliance 日历段](file:///c:/Users/Administrator/Desktop/a/index.html#L6177-L6214) |
| 10 分钟滴滴警报/大字 Modal/轮询频率 | [checkMeds](file:///c:/Users/Administrator/Desktop/a/index.html#L4614) + [playDidiAlert](file:///c:/Users/Administrator/Desktop/a/index.html#L4545) + [showMedAlertUI](file:///c:/Users/Administrator/Desktop/a/index.html#L4579) |
| 慢病档案强制选择/空态提示/家属端高亮规则 | `saveChronic` / `saveMetric` 入口处 check `selectedChronicId` |
| 趋势图（空白 / 闪烁 / 重复创建） | [drawFamilyChart](file:///c:/Users/Administrator/Desktop/a/index.html#L5232) + [_renderChartCore](file:///c:/Users/Administrator/Desktop/a/index.html#L6054) |
| 医院资质过滤 + 本地列表 + 高德搜索过滤 | [isQualifiedGeneralHospital](file:///c:/Users/Administrator/Desktop/a/index.html#L4700) / [validateHospitalInput](file:///c:/Users/Administrator/Desktop/a/index.html#L4718) |
| 家属绑定 + 拉取老人数据（只读，不写本地） | [bindFamily](file:///c:/Users/Administrator/Desktop/a/index.html#L5504) + [renderChronicSummary](file:///c:/Users/Administrator/Desktop/a/index.html#L5340) |
| 任何"读取某 store" / "写入某条记录" | `allMine('xxx')` 读，`DB.put('xxx', withUsername(rec))` 写；否则你可能写出串数据 bug |
| 日期对比 / 今日判定 | `nowBeijing()` + `beijingYMD()` 字符串比较，别用 UTC / toISOString / new Date(YYYY-MM-DD) |

---

## 12. 历史踩坑清单（改代码前先扫一遍是否中招）

| 曾经症状 | 根因 | 现在代码位置 |
|---|---|---|
| 26 号格子显示 25 号的数据，今天格子空 | 依从性日历 ds 用 UTC 日期，label 用本地月/日 → 凌晨差 1 天 | 统一 nowBeijing / beijingYMD，见 7.3 |
| 按钮点击后完成时间显示为 19:xx（实际凌晨 03:xx） | 从 actual_time 的 UTC 字符串截取了时分 | 改用 `actual_hhmm`（存的就是 beijingHHMM） |
| 通知里出现 `<b>` 原文 | showMedAlertUI(msg) 之前对 msg 做了 `<` 转义，但调用端传了含 HTML 的 msg | showMedAlertUI 改成 titleMsg/hintMsg 纯文本参数 |
| 家属端看了老人慢病后，老人端登录看到家属端自己账号里有老人 meds | 家属端错误把老人数据 DB.put 进本地 meds store | 现在家属端只读不写 + allMine/ensureMine 全链保护 |
| 按钮点了没反应 | onclick 字符串参数引号嵌套错误 + 重复声明 `let chronicChart = null` 触发 SyntaxError 导致整个 script 块解析失败，顶层函数未挂载 | 统一走 data 属性 + 委托；禁止重复 let/const |
| 趋势图 100% 空白 | Canvas 在 display:none 容器里 0x0 初始化 / 裸变量 `body` 未声明 ReferenceError / 切换视图未 destroy | 多级重试 + 固定兜底宽 + document.body && 判断 + 切视图先 destroy 旧实例 |
| 用药界面依从性空卡 | 仅 renderMeds 内部条件触发 renderCompliance，若切换视图/初始化未调用则空卡；且 DOM 不存在时无兜底创建 | go('med') / resetAppState / init 三处显式调用 renderCompliance，内部动态补 DOM + try/catch |
| 依从卡最新颜色画的是昨天的 | scheduled_date 存的北京时间 YYYY-MM-DD，但日历过滤用 UTC YYYY-MM-DD → 少 1 天 | 统一 beijingYMD 过滤 |
| 顶部"正在朗读"提示挤占慢病录入 toast | 该提示已被用户要求删除，已移除 | 若想恢复请不要重加（影响老人视觉注意力） |

---

**文档结束。改代码前请优先查看上面的章节对应位置、遵守时间处理规范和账号隔离三件套。祝接手顺利！**
